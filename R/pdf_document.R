pdf_document_old <- function(toc = TRUE,
                             number_sections = TRUE,
                             use.unsrturl = TRUE,
                             includes,
                             ...) {
  .defunct("pdf_document", old = "pdf_document_old")
}
