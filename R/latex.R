latex_old <- function(...,
                      width=90,
                      short.fignames=FALSE,
                      fig.path,
                      error=FALSE,
                      use.unsrturl=TRUE) {
  .defunct("latex", old = "latex_old")
}
